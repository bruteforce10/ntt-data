# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-05-27

## User Preferences

- (2026-07-19) Per-problem pitch decks are OPTIONAL: registrants may submit any subset; never gate submit on completeness (client canSubmit or API).
- (2026-07-19) PO ids (PO_01..PO_10) are internal markers: hidden on the public deck form; shown in the admin dashboard as column headers with the problem title as native `title` tooltip.
- (2026-07-19) Dashboard decks UI: separate PO_01..PO_10 columns (cell = 'Download' link) replace the combined 'Pitch Decks' column; legacy pick_deck reachable only via row actions dropdown + detail sheet.

<!-- How the user likes things done. Code style, tools, patterns, communication. -->
- [2026-06-02] NO inline `style={{ ... }}` on JSX. Sizing/layout via `<Image>` props (`width`/`height`) or static Tailwind classes only. User said: "saya gak mau menggunakan inline css tapi dari Image atau tailwindnya".
- Per-item image sizing from data: store numeric pixel `width`/`height` on the data object, pass directly as `<Image width={n} height={n}>` props. Avoid `fill` + sized wrapper for variable sizes.
- (2026-07-22) Submit errors must state the CAUSE in plain language, never a bare status code ("jangan sekedar nomor server error saja"). Every failure path gets its own explanation + next step: network→connection, 413→4.5 MB cap, PB field errors listed per field, 5xx→server message + code + support email. Helpers live in `lib/ntt-data/registration-errors.ts`.
- (2026-07-22) While a form submits, show a FULL-SCREEN blocking overlay (fixed inset-0 z-[100], LoaderCircle spinner) so nothing is clickable mid-upload ("gak bisa diapa2in pada proses submit").
- (2026-07-22) /startup-registration uses its OWN full-width bottom dock (`startup-registration-dock.tsx`: logo + nav + always-visible SUBMIT REGISTRATION button wired via native `form` attr to STARTUP_REGISTRATION_FORM_ID) — the shared pill FloatingDock is replaced on that page ONLY; other pages keep it.
- (2026-07-22) Pitch-deck (deck-submission) upload limit STAYS 8 MB despite Vercel's 4.5 MB body cap — explicit user decision; only the startup-registration company PDF was capped to 4.5 MB. Do not "fix" the deck limit unasked.
- [2026-07-15] Phone Number field = NO client-side validation at all. User first asked to loosen it for all countries, then corrected further: "kalau tidak di lolosin aja gak usah ada verifikasi tak ada logicnya" — any input passes, plain free-text field (type=tel + autoComplete=tel + placeholder showing +62/0895 examples only as hints). Raw value is sent; the API route (app/api/ntt-data/route.ts) normalizes to digits. Do not reintroduce format/length checks on optional contact fields.

- (2026-07-22) Dashboard table (`components/dashboard/columns.tsx` + `data-table.tsx`): show company_address, problem_statement, did_you_hear_about_us, country, city; long free-text cells use the `TextCell` helper (`max-w truncate` + `title` tooltip). Company description is ONE combined column — render the `company_description_pdf` download link when present, else the `company_description` text, never both. `business_mode` was removed from the dashboard entirely (table column, COLUMN_META, EXPORT_KEYS) per user; the PB field + registration API (`app/api/ntt-data/route.ts`) were intentionally left intact. Excel export must include funding_stage + both description fields; only `business_mode` was dropped from EXPORT_KEYS.

## Key Learnings

- **Excel export links (2026-07-23):** `recordsToAoa(records, keys, origin?)` in `lib/ntt-data/export-xlsx.ts` — PocketBase FILE fields store only the uploaded filename, so exporting `r[k]` raw gives the bare filename (useless in Excel). Fix: any COLUMN_META `type === "file"` column exports the ABSOLUTE download URL `${origin}/api/ntt-data/${id}/file/${field}`; `data-table.tsx` passes `window.location.origin`. `company_description` is ONE combined export column (mirrors the table cell): PDF link when `company_description_pdf` is set, else the plain `company_description` text — so `company_description_pdf` was dropped from `EXPORT_KEYS`. The download route requires an authenticated dashboard session, so links only resolve while logged in (acceptable for an admin export).

- **Project:** ntt-data
- Frontend direction: prioritize Next.js latest App Router, TypeScript, Tailwind CSS, and shadcn/ui components.
- Brand colors: button blue `#3176E4`, primary `#0070C0`, dark blue `#154284`.
- Tailwind v4 zero-config (no `tailwind.config.ts`/`.js`); content paths auto-detected. Dynamic class strings built from data files may not get scanned reliably — prefer static classes in JSX or props-based sizing.
- Next.js `<Image fill>` requires `sizes` prop and a sized wrapper; for variable per-item sizes, prefer numeric `width`/`height` props (no `fill`) to avoid `sizes` and aspect-ratio warnings.
- **shadcn uses the Base UI registry here, NOT Radix.** UI components in `components/ui/*` import from `@base-ui/react/*` (e.g. `@base-ui/react/dialog`, `/menu`, `/navigation-menu`, `/accordion`, `/button`). When porting code from a Radix-based shadcn project, translate the APIs:
  - `asChild` → `render={<El/>}` prop (Base UI composition). Children go on the Base UI component or inside the render element (one place only).
  - Base UI `Button` defaults `nativeButton: true`; when `render`-ing as `<a>`/`<span>`, pass `nativeButton={false}`.
  - DropdownMenu `Label` = Base UI `Menu.GroupLabel` → MUST be inside `<DropdownMenuGroup>`.
  - `<Accordion type="single" collapsible>` → `<Accordion multiple={false}>`. (NOT `openMultiple` — that prop doesn't exist in Base UI; TypeScript will error.)
  - `<NavigationMenu viewport={false}>` → no `viewport` prop (remove it); Base UI always renders a shared Positioner/Viewport.
- Some Base UI errors only throw on component **mount/open** (e.g. opening a menu), so a page-load console check misses them — verify interactively (open every dropdown/menu).
- designqc desktop viewport ≈ `--max-width` (default 1200px). The navbar's desktop layout uses the `xl` (1280px) breakpoint, so capture with `--max-width 1440` to see the full desktop nav instead of the mobile hamburger.
- For big banner-style text that scales to fill the width (mimicking a wide exported SVG/PNG), use a static Tailwind arbitrary class with clamp, e.g. `text-[clamp(2rem,11vw,9.5rem)]`. This is fluid like the SVG yet satisfies the "no inline style, static classes only" preference. `font-sans` already = NotoSans (`--font-sans: var(--font-noto-sans)` in globals.css), max weight loaded is 700 (`font-bold`).
- `openwolf` CLI is NOT on PATH in this environment (not a local/global npm bin), so `openwolf designqc` can't be auto-run here — verify UI in the browser instead.
- **Georgia local font** is loaded in `app/layout.tsx` via `next/font/local` from `public/georgia/` (4 ttf: `georgia`=400/normal, `georgiab`=700/normal, `georgiai`=400/italic, `georgiaz`=700/italic), exposed as CSS var `--font-georgia-base`. Used on the 5 section headings (About, Why Participate?, Program Overview, Registration Open, Explore the Challenges) via the `font-georgia` Tailwind utility, in `about.tsx`/`benefit.tsx`/`program-overview.tsx`/`countdown.tsx`/`problem-overview.tsx`.
- **Registering a NEW Tailwind v4 `font-*` utility:** the token in `@theme inline` must have a CONCRETE value, e.g. `@theme inline { --font-georgia: var(--font-georgia-base), Georgia, serif; }`. A self-referential `--font-georgia: var(--font-georgia)` (the trick used for the built-in `--font-sans`) does NOT generate a novel utility — Tailwind treats the circular value as empty and emits no `.font-georgia` rule, so the class silently becomes a no-op. Verify a new font utility actually rendered by cur-grepping the served CSS chunk for `.font-georgia {` (the dev-mode CSS is pretty-printed: match `\.font-x[[:space:]]*\{`, the `{` has a leading space).
- `public/Assets Picture/coming-soon.svg` = the words "COMING SOON" in brand blue `#0070C0`, drawn in a wide techno font (looks like the project's Nulshock), implemented as two embedded base64 PNGs (white letters used as a luminance mask over a solid `#0070C0` fill). To inspect such an SVG's true color/look, decode its base64 with PowerShell `[Convert]::FromBase64String` → write PNG → Read it; sample pixels via `System.Drawing.Bitmap.GetPixel`. The text version now used in `problem-overview.tsx` reproduces it with NotoSans.

- **FAQ is fully data-driven, FLAT (no group titles) as of 2026-07-04:** `components/faq.tsx` renders `SITE_CONTENT.faq` = `{title, subtitle, items[]}`. Each item is `{question, answer}` with OPTIONAL `steps?: string[]` (rendered as `<ol list-decimal>`) and `bullets?: string[]` (rendered as `<ul list-disc>`). ONE `<Accordion multiple={false}>` renders all items directly (value keys `faq-${i}`) — no per-group `<h3>` heading, no `groups` nesting. User explicitly asked to drop category grouping and merge everything into one list. `lib/home-faq.ts` (a separate curated shorter set) was DELETED — home page and `/faq` now render the SAME `SITE_CONTENT.faq` content via `<Faq />`'s default prop; `app/page.tsx` no longer passes a `content` override. To edit FAQ content, change `lib/site-content.ts` only. The same `<Faq />` is mounted on `app/faq/page.tsx` AND home `app/page.tsx` (bottom, before `<Footer />`), so `#faq` anchors work on both.
- **`as const` + optional fields gotcha:** `SITE_CONTENT` is `as const`, so mapping `faq.items` and accessing an optional `.steps`/`.bullets` that only SOME items have is a TS error (union of literal types). Fix: cast once to an explicit type — `const faqItems = faq.items as readonly FaqItem[]` where `FaqItem = {question:string; answer:string; steps?: readonly string[]; bullets?: readonly string[]}`. Keeps `as const` intact and lets `tsc` pass.
- **Base UI Accordion SSR:** collapsed `AccordionContent` panels render their children inside the React Server Component flight payload (`self.__next_f`, e.g. `["$","ol",null,{...}]`), NOT as literal `<ol>`/`<ul>` HTML on first paint. So `curl | grep '<ol'` returns 0 even though the list is present and hydrates correctly — grep the flight data for `list-decimal`/`list-disc` or the text instead.
- **Roadmap section** (`components/roadmap.tsx`, data in `SITE_CONTENT.roadmap` = `{title, phases:[{label, steps:[{caption, icon:{src,alt}}]}]}`): "Program Roadmap and Timeline", Server Component, mounted in `app/page.tsx` between `<ProblemOverview/>` and `<Countdown/>`. Desktop = CSS grid `grid-cols-[1fr_auto_1fr_auto_1fr]` with literal `col-start-*`/`row-start-1|2` placement (pills in row 1, step cells in row 2, inter-phase chevrons in the `auto` gap columns) so each pill spans its phase and chevrons line up with the icon row; mobile (`lg:hidden`) = vertical stack with `rotate-90` down-chevrons. Heading `font-georgia text-[#0070C0]` like sibling sections; band `bg-[#154284]`; pills `bg-linear-to-b from-[#2e5494] to-[#12376e]` + inset-highlight shadow. To keep Tailwind v4 from dropping runtime-selected classes, the per-column placement strings are LITERAL array entries (`["col-start-1","col-start-3","col-start-5"]`), not built with template strings.
- **`public/time-line/{1,2,3,4}.png`** = white glyphs centered on a 1920x1080 transparent canvas (glyph ≈92% of canvas height): 1=chat bubbles, 2=cloud upload, 3=doc+pen, 4=podium speaker. Render with `<Image width={1920} height={1080}>` + CSS height (`h-[72px]` desktop / `h-14` mobile) + `w-auto object-contain` — the transparent padding is invisible on the dark band and glyphs land at a consistent height with no aspect-ratio warning. `sharp(p).trim()` gives the glyph bbox (no ImageMagick/PIL here, but `sharp` is in `node_modules`).
- **Browser/visual verification in this env:** the Playwright MCP needs the "Playwright MCP Bridge" browser extension which is NOT connected (tools fail with "Extension connection timeout"), and `openwolf designqc` isn't on PATH. Workaround that works: drive Chrome headless via the DevTools Protocol from a node script — node here is v24 with global `WebSocket` + `fetch`. Launch `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --remote-debugging-port=9222 --user-data-dir=<tmp>`, GET `http://127.0.0.1:9222/json` for the page target's `webSocketDebuggerUrl`, then `Page.navigate` + poll `document.readyState` + `Runtime.evaluate` to `scrollIntoView` the target section and `Page.captureScreenshot` the viewport. Use `Emulation.setDeviceMetricsOverride` for mobile (390×844, dsf 2, mobile:true).

- **Mobile verification (2026-07-16): `mcp__claude-in-chrome__resize_window` does NOT resize the page viewport** — it reports success and shrinks the OS window (`outerWidth` 784) while `window.innerWidth` stays 1680 and `matchMedia("(min-width:640px)")` still matches, so `sm:` breakpoints never flip and screenshots stay desktop-width. Don't burn turns retrying it. Working path: `npm i puppeteer-core` INTO THE SCRATCHPAD (not the project — keeps package.json clean; no browser download needed) and drive the system Chrome at `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` with `page.setViewport({width:390,height:844,deviceScaleFactor:2,isMobile:true})`. Modals are React-state-driven (not URL-driven), so open them with `page.evaluate` → find the card by `textContent` → `.click()`, wait ~1.2s, then assert real overflow via `scrollWidth > clientWidth` while skipping intentional `overflow-x:auto` scrollers.
- **problemOverview.items data conventions** (`lib/site-content.ts` + `components/problem-overview.tsx`, 2026-07-10, updated 2026-07-11): store `detail.description.develop` as a NOUN PHRASE ("An integrated ... solution that ...") — the modal prints the italic "Develop:" label itself, so a leading "Develop ..." verb would double up. `develop` accepts `string | readonly string[]`; an array renders as one `<p>` per entry (`space-y-3`), used by the Kajima/dHost items to show the develop text + "Tailored towards ..." line as two paragraphs. Same for `tailoredTowards` (store audience phrase only; component prints "Tailored towards:") and `keywords` (single string of hashtags; component prints "Keywords:"). Registration deep-links use the array INDEX (`/startup-registration?problem=<i>`), so avoid reordering casually — the card grid and the registration form checkboxes both derive from the same array (stay in sync automatically; submissions store TITLES not indices), but previously shared external `?problem=N` URLs silently point at a different problem after a reorder. [2026-07-11] User-requested canonical order is now: 1 Supply Chain (industrial materials), 2 AI Knowledge Platform (bank), 3 Digital Twins (bank), 4 dHost Edge AI, 5 Kajima I Asset Handover, 6 Kajima II Observability. Kajima logoLabels are suffixed "I"/"II" to disambiguate. New optional modal sections follow the `{field && (<> label + content </>)}` pattern; card shows only logoLabel + title (item.description is currently unrendered metadata).
- **`richDetail`: the second problem-statement modal layout (2026-07-16).** A `problemOverview.items` entry carries EITHER `detail` (standard: context paragraphs + description lists, rendered inline in `problem-overview.tsx`) OR `richDetail` (long-form doc, rendered by `components/problem/problem-rich-detail.tsx`). The right panel branches `richDetail ? <ProblemRichDetail> : detail ? (...) : null`, so both shapes coexist and existing items are untouched. `richDetail` mirrors the source document section-by-section (`trades`/`broken`/`solve`/`dependencyChain`/`capabilities`/`requirements`) rather than a generic block array — deliberate YAGNI, only one item needs it. Presentation stays OUT of the data: numbering (1-6 pain points, 01-07 modules) and module colours derive from array index; only `trade: "cooling"|"cabling"|"nodes"` is stored, because that colour is a LEGEND shared by the trade cards and the dependency chain. Item 7 = "Unified Orchestration for Multi-Trade Data Centre Delivery" (APPENDED, so the 1-6 canonical order and existing `?problem=N` links are unaffected).
- **WCAG gotcha — brand button blue `#3176e4` FAILS AA on white text (4.34:1 < 4.5) at normal size.** Fine for large/bold ≥18.66px text, for non-text UI (the round arrow buttons, borders, focus rings), and for dark text on it — but never for small white labels. AA-safe brand fills for white text: `#0070C0` 5.15:1, `#154284` 9.79:1, `#2560c8` 5.86:1 (already in the codebase as the `#3176e4` hover in `hero.tsx`/`floating-dock.tsx`). That trio is the go-to when a design needs three distinguishable on-brand colour blocks. Compute, don't eyeball: linearise sRGB then `(L1+0.05)/(L2+0.05)`.
- **Headless-Chrome CDP verification on Windows** (this env, 2026-07-10): same trick as the macOS entry below but Chrome is at `C:\Program Files\Google\Chrome\Application\chrome.exe` (Edge msedge.exe also available); node is v22 (global `fetch` + `WebSocket` work). Wait ~3s after `readyState==='complete'` for React hydration before `element.click()`; Base UI Dialog closes via `Input.dispatchKeyEvent` Escape; clicking another card while a dialog is open just swaps its content (controlled `open={!!selected}`), so a stuck dialog self-heals.

- **SEO/canonical config (2026-07-15):** single source of truth = `lib/site-config.ts` (`SITE_URL` from `NEXT_PUBLIC_SITE_URL`, fallback `https://oiw.ntt-startupchallenge.com`). Domain map: apex `ntt-startupchallenge.com` = SEPARATE parent site (different Next app with Clerk/GA — do not treat as this app); `pb.ntt-startupchallenge.com` = PocketBase; `oiw.` = THIS sub-site (user-confirmed; DNS not yet registered as of 2026-07-15). `NEXT_PUBLIC_BASE_URL` is for email CTA links and points at `pb.` in local `.env` — never use it for canonicals/OG.
- **Next.js metadata merging is SHALLOW:** a page-level `openGraph`/`robots` object REPLACES the layout's whole object (no deep merge). Convention here: root `app/layout.tsx` owns the complete `openGraph` (without `url`); pages set only `title`, `description`, `alternates.canonical` (relative, resolved via `metadataBase`), and `robots` noindex where needed. Twitter card auto-inherits from OG. Regenerate og-image/icons with `node scripts/generate-seo-assets.js`.

- **problemOverview modal — two NEW optional description sections (2026-07-18):** `ProblemDetailDescription` (in `components/problem-overview.tsx`) now supports `painPoints?` (label "Main pain points:") and `lookingFor?` (label "What we are looking for:"), rendered with the same guarded `{field && (<p italic label> + <ol list-decimal>.map)}` pattern as the other optional sections (targetOutcomes/dataExpected/priorityScope). ALSO: `helpsUsers` and `solutionMust` are now OPTIONAL (previously required) and each render is now guarded — an item may omit either without a crash. All 7 pre-existing items still supply both, so no regression. Two items were APPENDED (indices 7 & 8, keeping existing `?problem=N` links valid): #8 "AI-Powered Autonomous Telco Assurance and Operations" with `logoLabel: ""` (empty → card & modal correctly render the title only, via the existing `{item.logoLabel && …}` guards) using develop/helpsUsers/solutionMust; #9 "Real-Time Predictive Quality … GxP Pharmaceutical Manufacturing" (logoLabel "A Fortune 500 global pharmaceutical and healthcare company") using develop/painPoints/targetOutcomes/lookingFor. Render order is fixed by the component: develop → painPoints → question → helpsUsers → priorityScope → targetOutcomes → dataExpected → solutionMust → lookingFor → pocApproach → keywords. Verified both modals open via headless Chrome with positive + negative label assertions all passing.
- **Headless-Chrome CDP (2026-07-18 refinement of the macOS recipe):** with `--headless=new`, the `/json` targets list is EMPTY until a page exists — pass the target URL as a positional launch arg (or call `Target.createTarget`) so a `type:"page"` target appears; otherwise the `webSocketDebuggerUrl` lookup returns nothing ("no CDP target"). `/json/version` (the browser-level WS) is reachable within ~3s regardless. Node v24 here has global `fetch` + `WebSocket`.

- **Per-problem pitch decks (2026-07-19):** PocketBase `ntt_data` has TEN file fields `PO_01`..`PO_10` (underscores — PB field names can't contain "-"). `problemOverview.items[].id` in site-content uses the SAME `PO_01`..`PO_10` format (user request 2026-07-19: id = PB field name exactly; originally "PO-01" with hyphen). Single source of truth = `lib/problem-decks.ts`: `PROBLEM_DECKS` (id→field→title→logoLabel), `matchProblemDecks(problem_statement)` recovers the registrant's selection by TITLE SUBSTRING (registration stores titles joined ", "; titles contain no commas and none is a substring of another — both invariants guarded by `tests/ntt-data/problem-decks.test.ts`, so keep them when editing titles). Flow: deck form GETs `/api/deck-submission?email=` → `{problems:[{id,uploaded}]}` → renders ONE upload input per selected problem (input `name` = PB field `PO_xx`; required unless `uploaded`; re-visits allow optional replace); POST accepts multi-file keyed by `PO_xx`, enforces files ⊆ selected + completeness (existing upload counts), then ONE multipart PATCH + one confirmation email. Dashboard: combined "Pitch Decks" column (per-PO links + "Legacy deck" for old `pick_deck`), per-PO rows in RecordDetail via COLUMN_META, `/api/ntt-data/[id]/file/[field]` accepts PO fields via `NTT_DATA_FILE_FIELDS`, Excel export includes the 10 PO columns.
- **Local PocketBase creds scope (2026-07-19, supersedes the "empty local env" note):** `.env.local` now has POCKETBASE_URL + a 33-char POCKETBASE_SUPERUSER_TOKEN that WORKS for record operations (list/filter returned real 404-not-found flows) but gets 401 on `/api/collections/ntt_data` (schema introspection needs a real superuser JWT). So: verify data flows locally, but don't try to read the collection schema with it.
- **GateGuard hooks in this env:** the FIRST Bash/Write/Edit per session/file is blocked by a "[Fact-Forcing Gate]" asking for facts; present the facts in the visible reply, then RETRY the identical call — it passes. `rm -rf` (destructive) always asks for a deletion list + rollback line. Loop warnings from repeated Edit retries are false positives caused by these gates.
- **Multi-machine workflow (2026-07-22):** User pushes to `feat/ntt-data-dashboard` from at least two machines (this Mac + a Windows box, paths `C:/Users/jayac/...`). The OpenWolf log files (`.wolf/hooks/_session.json`, `.wolf/memory.md`, `.wolf/token-ledger.json`) are committed on BOTH machines and conflict on every cross-machine sync. Resolution recipe (see buglog bug-066): `git pull --rebase`; `_session.json` → keep the newest session block; `memory.md` → keep both session blocks in chronological order; `token-ledger.json` → take the max of each lifetime counter. Consider proposing `.gitignore` for these files if conflicts recur.

- (2026-07-23, Key Learning) GateGuard fact-force hook ALWAYS blocks the first Edit/Bash per file per session even when facts precede it in the same message; the identical retry next message passes. Expect exactly one block per new file and batch retries.
- (2026-07-23, Key Learning) React Doctor `rerender-state-only-in-handlers` flagged registeredEmail which was fully DEAD state (set, never read) - right fix was deletion, not useRef. Verify usages with case-INSENSITIVE grep (`setFoo` hides the lowercase name).
- (2026-07-23, Key Learning) React Doctor reentry-guard rule is NOT satisfied by a state-value check (`if (status==='submitting') return`); it wants a synchronous ref guard (isSubmittingRef) - which is also the genuinely race-free pattern since state reads are stale between renders.
- (2026-07-23, Key Learning) Native <dialog> pattern (deck-submission-form): ref + effect syncing showModal()/close() to state, onClose handler syncs state back (covers native Esc), backdrop-click = e.target===e.currentTarget with dialog styled p-0 bg-transparent and an inner card div; Tailwind `backdrop:bg-black/60`. Note: backdrop onClick trips react-doctor no-noninteractive-element-interactions (validated FP - Esc is the keyboard path).

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-06-02] Don't use inline `style={{ width, height }}` on Image wrappers or dynamic Tailwind class strings (`h-${n}`, `w-${n}`) read from `.ts` data files. User explicitly rejected inline CSS. Instead: pass `width`/`height` as numeric props to `<Image>` directly. Drop `fill` + wrapper unless size is fixed at design time with static Tailwind classes.
- [2026-06-07] Don't paste Radix-based shadcn code (`asChild`, Accordion `type/collapsible`, NavigationMenu `viewport`) into this project — it uses the **Base UI** shadcn registry (`@base-ui/react`). It compiles but renders broken ("like shadcn isn't installed"). Use Base UI APIs instead (see Key Learnings).
- [2026-06-07] When `render`-ing a Base UI `Button` as a non-button (`<a>`/`<span>`), always set `nativeButton={false}` or it logs a console error (Next dev shows "1 Issue").
- [2026-06-07] Base UI `DropdownMenuLabel` (Menu.GroupLabel) throws unless wrapped in `<DropdownMenuGroup>`; error only appears when the menu is opened, so verify dropdowns by actually opening them.
- [2026-06-08] Base UI Accordion root prop for single-open behavior is `multiple={false}`, NOT `openMultiple={false}`. Using `openMultiple` causes a TS build error on Vercel.
- [2026-06-27] Don't register a NEW Tailwind v4 `font-*` utility with a self-referential `@theme inline { --font-x: var(--font-x); }`. It compiles with no error but generates NO utility class, so the `font-x` class is a silent no-op (text keeps the inherited font). Use a concrete value referencing the next/font variable instead, e.g. `--font-georgia: var(--font-georgia-base), Georgia, serif;`. The self-ref only "works" for built-ins like `font-sans`/`font-serif`/`font-mono` that Tailwind ships by default. (see buglog bug-068)
- [2026-06-27] Don't screenshot a mid-page section via an absolute `clip` computed from `getBoundingClientRect` right after `Page.navigate` — unloaded (lazy) images above collapse the layout so the reported offset is wrong (it captured the Hero instead of `#roadmap`). Instead `scrollIntoView` the section, wait for images, then capture the VIEWPORT (no clip). Also: the first headless request after editing triggers a cold Turbopack compile that can exceed the screenshot budget and yield a blank page — warm the page with a `curl` first.

- [2026-07-19] Don't verify client-side behavior (hydration, effects, fetch) against the USER'S running `next dev` on :3000 — it's instrumented by Console Ninja (editor extension) and React never hydrates in headless Chrome (page-wide: zero `__reactFiber` keys, page stuck on its SSR HTML, no console errors). Also `next dev` refuses a second instance for the same dir. Instead: `npm run build` + `PORT=3200 npx next start`, verify there, then kill only the :3200 pid. (see buglog bug-050)
- [2026-06-27] Don't put a raw bcrypt hash (`$2b$10$...`) in `.env.local` — Next's `@next/env` runs dotenv-expand and treats `$2b`/`$10`/etc. as variable refs, corrupting it (loads empty/partial). **Single-quoting does NOT prevent expansion** in this version. Fix: escape every `$` as `\$` (`\$2b\$10\$...`). Symptom: Auth.js `CredentialsSignin` for the CORRECT password while the unit test (hash passed directly, not via env) passes green. (see buglog bug-090)

## Decision Log

<!-- new entries 2026-07-23 -->
- [2026-07-23] User chose to KEEP all 6 react-doctor-flagged unused files (program-overview, ui/carousel, ui/navigation-menu, ui/popover, constant/listNavbar, utils/eachUtils) AND the 4 unused @tiptap/* deps. Do NOT re-suggest deleting them; their unused-file/unused-dependency findings are accepted noise. program-overview's trivial findings (keys, button type, sizes, lazy plugin init) were fixed anyway in case it returns to the page.

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- [2026-07-15] SEO canonical domain = `https://oiw.ntt-startupchallenge.com`, chosen by user over apex (apex serves the separate parent site). Wired through `NEXT_PUBLIC_SITE_URL` env + fallback in `lib/site-config.ts` so a future domain change is a 1-line edit. robots.txt disallows `/api/` + `/dashboard` but NOT `/login` — login relies on its noindex meta, which crawlers can only see if the page stays crawlable.
- [2026-07-13] Navbar simplified per user request: the LIST_NAVBAR nav list is removed on BOTH desktop and mobile (mobile Sheet/Accordion/hamburger deleted entirely). Navbar = logo + "More" dropdown + Open Innovation button at every breakpoint, with responsive sizing (logo `h-12 sm:h-16 md:h-24`, OI logo `h-5 sm:h-6`, tighter mobile padding) — verified no horizontal overflow at 360/390px via headless Chrome. Page-section navigation is handled by the floating dock only. `constant/listNavbar.ts` and `utils/eachUtils.tsx` are now unreferenced (left in repo). Do not reintroduce the nav list or hamburger unless asked.
- [2026-06-07] Ported the parent NTT Startup Challenge navbar into the sub-site by **adapting it to Base UI** (the project's existing shadcn registry) rather than installing `@radix-ui/*` and swapping the UI components. Rationale: keeps the project consistent on one component library and avoids replacing the Base-UI `button.tsx` that other components (forms, floating-dock) already depend on. Register CTA uses brand `#3176E4` per CLAUDE.md (also matches the design image) instead of the parent's `blue-ntt`. Desktop nav kept at the `xl` breakpoint (7 items + 2 buttons don't fit below ~1280px).

- **Verifying the dashboard locally:** local .env has EMPTY `POCKETBASE_URL`/`POCKETBASE_SUPERUSER_TOKEN`, so /dashboard always renders the 'Failed to load registrations' error branch locally — dashboard tables can only be seen with real data in production. Workaround for UI verification: temp preview route with mock `NttDataRecord`s (delete after). Programmatic NextAuth login for HTTP tests works: GET /api/auth/csrf → POST /api/auth/callback/credentials (form-encoded csrfToken+email+password, keep cookies) → session cookie. SSR HTML splits interpolated text with hydration comments (`Page <!-- -->1`), so strip `<!--...-->` before substring checks.

- (2026-07-22, Key Learning) PocketBase `=` filter is CASE-SENSITIVE on text fields; the live instance supports the `:lower` field modifier (`email:lower='...'` with lowercased input) for case-insensitive matching. `~` is a substring LIKE, not an equality alternative.
- (2026-07-22, Key Learning) Registration flow has NO user accounts: "login" in user reports = the deck-submission email gate (`/deck-submission?email=`), which looks up the ntt_data record by email. ntt_data allows duplicate emails (no unique index).
- (2026-07-22, Key Learning) React Doctor per-rule canonical recipes at `www.react.doctor/prompts/rules/react-doctor/<rule>.md` RESOLVE as of 2026-07-22 (earlier 404s were transient — always retry this URL first); only if it 404s fall back to the suggestion text in the scan's per-rule .txt files + own judgment. Fetch-status-check fix shape used repo-wide: check `res.ok` FIRST, parse error payload inside the `!ok` branch, parse success body after.
- (2026-07-22, Key Learning) position:fixed elements never grow document scrollWidth, so a mobile `scrollWidth > clientWidth` overflow check CANNOT see a fixed bar's clipped children (the dock CTA was cut off at 390px while the check passed). Assert `Math.ceil(rect.right) <= window.innerWidth` on the widest child instead — and always eyeball the screenshot.
- (2026-07-22, Key Learning) A button OUTSIDE a form submits it via the native `form="<form-id>"` attribute — works with React onSubmit + noValidate client validation, no context/ref plumbing needed (used by the registration dock CTA).
- (2026-07-22, Key Learning) React Doctor `no-deprecated-tailwind-class` misses classes inside `cn(...)` args (roadmap.tsx) and template-literal classNames (problem-rich-detail.tsx `sm:flex-shrink-0`). When fixing that rule, grep `bg-gradient-to-|flex-shrink-|flex-grow-|overflow-ellipsis` to catch every site, not just the flagged ones. Also its scan snapshot line numbers drift vs the working tree — trust grep, not the report's line numbers.
